name: iOS Deploy

on:
  push:
    branches:
      - 'qa-dev'

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "15.1"

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.0"

      - name: Install Fastlane
        run: sudo gem install fastlane

      - name: Setup environment variables
        run: echo "MATCH_GIT_BASIC_AUTHORIZATION=${{ secrets.ACCESSTOKENFORGITHUBBASE64 }}" >> $GITHUB_ENV

      - name: Set up Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.19.2"
          channel: "stable"
          architecture: x64
          cache: true



      - name: Build iOS
        run: flutter build ios --release --no-codesign

      - name: Build and deploy to TestFlight
        env:
          ACCESSTOKENFORGITHUB: ${{ secrets.ACCESSTOKENFORGITHUB }}
          ACCESSTOKENFORGITHUBBASE64: ${{ secrets.ACCESSTOKENFORGITHUBBASE64 }}
        working-directory: ./ios
        run: fastlane deployToTestFlight

